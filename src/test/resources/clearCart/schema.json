{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "required": ["ack", "message", "data"],
  "properties": {
    "ack":      {"type": "string", "enum": ["0"]},
    "message":      {"type": "string", "enum": ["Successfully items are cleared from the cart"]},
    "data": {"type": "object","required":["time_stamp", "_id", "guid", "store_id", "items"], "properties":{
         "time_stamp": {"type":"integer"},
         "_id":{"type":"string"},
         "guid": {"type": "string"},
         "store_id":{"type":"string"},
         "items" :{"type":"array"},
         "__v": {"type": "integer"}
         
         
    }
  }

  }
}