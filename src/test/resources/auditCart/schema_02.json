{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties":{ 
    "ack":      {"type": "string", "enum":["1"]},
    "message":  {"type": "string", "enum":["Order details not found. Please check the barcode and try again."]}
   },
    "required": ["ack","message"]
}