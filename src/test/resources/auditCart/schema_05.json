{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties":{ 
    "ack":      {"type": "string", "enum":["0"]},
    "message":  {"type": "string", "enum":["Audited"]}
   },
    "required": ["ack","message"]
}