{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
     "ack" : {
          "type": "string", "enum":["1"]
        },
       "errors": {"type": "array","items": [
        {
          "type": "object",
          "properties": {"code": {"type": "string", "enum": ["4004"]},
            "message":  {"type": "string", "enum": ["This item is not supported. Please go to the regular checkout."]},
            "type":     {"type": "string", "enum": ["SNG_database"]},
            "category": {"type": "string", "enum": ["item_not_supported"]},
            "vendor":   {"type": "string", "enum": ["SNG backend"]}
          },
          "required": ["code","message","type","category","vendor"]
        }
      ]
    }
  },
  "required": [
    "ack",
    "errors"
  ]
}

