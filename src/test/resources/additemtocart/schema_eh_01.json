{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "ack":      {"type": "string", "enum": ["0"]},
    "code":     {"type": "string", "enum": ["4000"]},
    "message":  {"type": "string", "enum": ["Item not added to cart as quantity is zero"]},
    "vendor":   {"type": "string", "enum": ["Scan_and_go"]},
    "category": {"type": "string", "enum": ["generic_error"]}
  },
  "required": ["ack","code","message","vendor","category"]
}
